# .env.example
# Copy to .env.local and add your Anthropic API key.
# NEVER commit .env.local — it is already in .gitignore.
#
# This key enables riley.brain = 'claude' in fogsift.config.js.
# If not set, the game runs in static mode (no Claude API needed).
#
# Get a key: https://console.anthropic.com/

VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
